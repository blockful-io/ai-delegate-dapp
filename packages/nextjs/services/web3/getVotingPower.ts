export const getVotingPower = (address: string, contractAddress: string): number => {
  // TODO: update this to fetch voting power from the blockchain
  console.log(address, contractAddress);
  return 1;
};
